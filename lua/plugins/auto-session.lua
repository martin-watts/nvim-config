return {
  -- auto-session automatically saves/restores
  -- sessions (open buffers, file positions etc) based on the
  -- current working directory when neovim starts
  { "rmagatti/auto-session" },
}
